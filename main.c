#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int addend=3 ,augend=2,sum=0;//被加數和加數和總和 
	sum=augend+addend;//3+2
	printf("sum=%d",sum);
	return 0;
}
